#include "main.h"
/**
 * _islower-check if the letter is lower case or not
 *
 * Return: Always 0.
 */
int _islower(char c)
{
int checker = 0;
for(int i=49; i<=74; i++)
{
if(_putchar(i+'0') >= i)
{
checker =1;
return (checker);
}
else
{
return (checker);
}
}
_putchar('\n');
}

