#include "main.h"
/**
 *  _abs - computes the absolute value of an integer
* @number:  is an ascii character
 * Return: Always 0.
 */
int _abs(int number)
{
if (number > 0)
{
return (number);
}
else if (number < 0)
{

return (-number);
}
else
{
return (0);
}
}

