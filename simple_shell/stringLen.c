#include "shell.h"

/**
 *_strlen - function that counts the length of a string
 *@s: string input
 *Return: length of string
 */

int
stringLen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}

	return (i);
}
