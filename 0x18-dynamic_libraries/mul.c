#include "test.h"
/**
 *mul - multiply 2 number
 *@a: is first number
 *@b: is second number
 *Return: Always 0.
 */
int mul(int a, int b)
{
int mul_result;
mul_result = a * b;
return (mul_result);
}
