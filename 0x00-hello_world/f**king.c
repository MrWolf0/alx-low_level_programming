#include <stdio.h>

/**
* main is the start point in the program
*
* return (0) if successfully execution
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
